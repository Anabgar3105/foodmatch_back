package edu.abga.foodmatch.model;

/**
 * Enum que define los roles de usuario disponibles en el sistema.
 * Restringe los valores posibles para la columna 'role' en la base de datos.
 */
public enum Role {
    USER,
    ADMIN
}